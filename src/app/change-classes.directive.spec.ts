import { ChangeClassesDirective } from './change-classes.directive';

describe('ChangeClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeClassesDirective();
    expect(directive).toBeTruthy();
  });
});
